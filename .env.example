DATABASE_URL=sqlite:///./data/backlog.db
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-long-random-password
# OData access uses the Windows account running FastAPI. Do not store BC credentials here.
